/*------------------------------------------------------------------------------------
filename.ino

Some description of functionality

Optional Link to relevant documentation

Created: mm/dd/yy - Name - email@louisiana.edu

Modified:
  * mm/dd/yy - Name (email if not same person as above)
    - major change 1
    - major change 2
  * mm/dd/yy - Name (email if not same person as above)
    - major change 1
------------------------------------------------------------------------------------*/

// Variables declared here are global
int globalVariable;    // example declaration, globalVariable will be availble to all

// This is always run once when the sketch starts
// Use to initialize variables, pin modes, libraries, communication, etc
void setup()
{
  // variables declared inside a function are only available there
  byte setupVariableOnly;    // This byte variable is only available in steup()
  
  // Your setup code goes here
}


// This runs immediately after setup, looping indefinitely
void loop()
{
  // variables declared inside a function are only available there
 boolean loopVariableOnly;    // This boolean variable is only available in loop()
  
  // Your main code goes here
}
